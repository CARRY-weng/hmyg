<view class='address_view'>
    <!-- // 如果本地存储中没有地址 就显示按钮   这里的判断条件要加 .属性 因为就算是{} 布尔类型也是true -->
    <button wx:if='{{!address.uesrName}}' bindtap='handleGetAddress'>
        获取收货地址
    </button>
    <view wx:else>
        {{address.uesrName}}
    </view>
      
</view>
  

