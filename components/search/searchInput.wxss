.searchInput_warp {
  background: var(--themeColor);
  padding: 10rpx 10rpx;
  height:80rpx;
}
.search{
    /* 设计稿为375 的时候 1px = 2rpx */
    background: #fff;
    /* padding: 4px 10px; */
    border-radius: 8px;
    color: #999;
    font-size: 24rpx;
    text-align: center;
    height:60rpx;
    line-height: 60rpx;
}